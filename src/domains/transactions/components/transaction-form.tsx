"use client";
import Link from "next/link";
import { useActionState, useState } from "react";
import type { Category, FinancialAccount, Transaction } from "@/types/database";
import { FieldError, FormMessage, SubmitButton } from "@/components/auth/form-controls";
import { createTransactionAction, createTransferAction, updateTransactionAction } from "../actions";
import { initialTransactionState } from "../state";
type Mode = "income" | "expense" | "transfer";
export function TransactionForm({ personId, accounts, categories, transaction }: { personId: string; accounts: FinancialAccount[]; categories: Category[]; transaction?: Transaction }) { const [mode, setMode] = useState<Mode>(transaction?.transaction_type === "income" ? "income" : "expense"); const action = transaction ? updateTransactionAction.bind(null, personId, transaction.id) : mode === "transfer" ? createTransferAction.bind(null, personId) : createTransactionAction.bind(null, personId); const [state, formAction] = useActionState(action, initialTransactionState); const activeAccounts = accounts.filter((a) => a.status === "active"); const usableCategories = categories.filter((c) => c.active && (c.usage === mode || c.usage === "both")); return <form action={formAction} className="space-y-5">{!transaction && <div className="grid grid-cols-3 gap-2" role="group" aria-label="Type d’opération">{([['income','Recette'],['expense','Dépense'],['transfer','Virement']] as const).map(([value,label]) => <button key={value} type="button" onClick={() => setMode(value)} className={`button ${mode === value ? "button-primary" : "button-secondary"}`}>{label}</button>)}</div>}{mode === "transfer" && !transaction ? <><input type="hidden" name="kind" value="transfer" /><DateField name="transferDate" label="Date" errors={state.fieldErrors?.transferDate} /><AccountSelect name="sourceAccountId" label="Compte source" accounts={activeAccounts} errors={state.fieldErrors?.sourceAccountId} /><AccountSelect name="destinationAccountId" label="Compte destination" accounts={activeAccounts} errors={state.fieldErrors?.destinationAccountId} /><Field name="amount" label="Montant" type="number" step="0.01" errors={state.fieldErrors?.amount} /><Field name="label" label="Libellé facultatif" errors={state.fieldErrors?.label} /><Textarea name="comment" label="Commentaire facultatif" /></> : <><input type="hidden" name="transactionType" value={transaction?.transaction_type ?? mode} /><AccountSelect name="financialAccountId" label="Compte" accounts={accounts} defaultValue={transaction?.financial_account_id} errors={state.fieldErrors?.financialAccountId} /><DateField name="transactionDate" label="Date" defaultValue={transaction?.transaction_date} errors={state.fieldErrors?.transactionDate} /><Field name="label" label="Libellé" defaultValue={transaction?.label} errors={state.fieldErrors?.label} /><Field name="amount" label="Montant" type="number" step="0.01" defaultValue={transaction?.amount} errors={state.fieldErrors?.amount} /><div><label className="auth-label" htmlFor="categoryId">Catégorie facultative</label><select className="auth-input" id="categoryId" name="categoryId" defaultValue={transaction?.category_id ?? ""}><option value="">Sans catégorie</option>{usableCategories.map((c) => <option key={c.id} value={c.id}>{c.name}</option>)}</select></div><Field name="proofReference" label="Référence justificatif facultative" defaultValue={transaction?.proof_reference} /><Textarea name="comment" label="Commentaire facultatif" defaultValue={transaction?.comment} /></>}<FormMessage state={state} /><div className="flex flex-col-reverse gap-3 sm:flex-row sm:justify-end"><Link href={`/dossiers/${personId}/operations`} className="button button-secondary">Annuler</Link><div className="sm:min-w-44"><SubmitButton pendingLabel="Enregistrement…">{transaction ? "Enregistrer" : mode === "transfer" ? "Créer le virement" : "Ajouter l’opération"}</SubmitButton></div></div></form>; }
function AccountSelect({ name, label, accounts, defaultValue, errors }: { name: string; label: string; accounts: FinancialAccount[]; defaultValue?: string; errors?: string[] }) { return <div><label className="auth-label" htmlFor={name}>{label} *</label><select className="auth-input" id={name} name={name} required defaultValue={defaultValue ?? ""}><option value="" disabled>Choisir un compte</option>{accounts.map((a) => <option key={a.id} value={a.id}>{a.account_name} — {a.institution_name}</option>)}</select><FieldError messages={errors} /></div>; }
function DateField({ name, label, defaultValue, errors }: { name: string; label: string; defaultValue?: string; errors?: string[] }) { return <Field name={name} label={label} type="date" defaultValue={defaultValue ?? new Date().toISOString().slice(0,10)} errors={errors} />; }
function Field({ name, label, type="text", step, defaultValue, errors }: { name: string; label: string; type?: string; step?: string; defaultValue?: string | number | null; errors?: string[] }) { return <div><label className="auth-label" htmlFor={name}>{label} *</label><input className="auth-input" id={name} name={name} required={!label.includes("facultati")} type={type} step={step} min={type === "number" ? "0.01" : undefined} defaultValue={defaultValue ?? ""} /><FieldError messages={errors} /></div>; }
function Textarea({ name, label, defaultValue }: { name: string; label: string; defaultValue?: string | null }) { return <div><label className="auth-label" htmlFor={name}>{label}</label><textarea className="auth-input min-h-24 py-3" id={name} name={name} defaultValue={defaultValue ?? ""} /></div>; }
